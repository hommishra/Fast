rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Enable global administrative and public sandbox permissions
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
